============
Installation
============

At the command line::

    $ easy_install django-tweet

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-tweet
    $ pip install django-tweet
